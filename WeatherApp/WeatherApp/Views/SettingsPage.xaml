<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resource="clr-namespace:WeatherApp.Resources"
             x:Class="WeatherApp.Views.SettingsPage"
             BackgroundColor="#758ABA">
    <ContentPage.Content>
        <StackLayout Padding="20" VerticalOptions="CenterAndExpand">

            <Label  Text="{x:Static resource:AppResources.SelectLanguage}"  TextColor="White"/>
            <Picker x:Name="picker1" HorizontalOptions = "StartAndExpand" 
                    WidthRequest="400"
                    TextColor="White"/>
            <Label  Text="{x:Static resource:AppResources.SelectMetrics}"  TextColor="White"/>
            <Picker x:Name="picker2" HorizontalOptions = "StartAndExpand" 
                    WidthRequest="400"
                    
                    TextColor="White"/>
            <Button Text="{x:Static resource:AppResources.Save}"
                       Clicked="OnUpdateSaveClicked"
                       BackgroundColor="White"
                    
                       TextColor="#758ABA"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>